services:
  forgejo-runner:
    command: forgejo-runner -c /config.yml daemon
    #command: '/bin/sh -c "while : ; do sleep 1 ; done ;"'
    container_name: forgejo-runner1
    image: code.forgejo.org/forgejo/runner:13@sha256:c4af85fd9f0dd03788676a534781a87c71aa2c6a37737143e017eb94d4312952
    networks:
      - proxy
    ports:
      - 6773:6773
    group_add:
      - 112
    restart: unless-stopped
    environment:
      -  FORGEJO_INSTANCE_URL=https://{{ FORGEJO_HOST }}
      -  FORGEJO_REGISTRATION_TOKEN={{ FORGEJO_RUNNER_REG_TOKEN }}
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./forgejo-runner/data:/data
      - ./forgejo-runner/config.yml:/config.yml:ro
      - mkdocs-site:/site

volumes:
  mkdocs-site:
    external: true
